.content_post
  .tweet-user-name
    = tweet.user.name
  .tweet-text
    = tweet.text
  .more
    = image_tag tweet.image.url, class: "tweet-image" if tweet.image.present?
    .more_list
      = link_to '削除', tweet_path(tweet.id), method: :delete